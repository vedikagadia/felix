# AWS Bedrock (reasoning + embeddings)
boto3>=1.34

# LLM reasoning (LLM_PROVIDER=gemini)
google-generativeai>=0.7

# Local embeddings (EMBED_PROVIDER=local): BAAI/bge-large-en-v1.5, 1024-dim
sentence-transformers>=3.0

# CockroachDB (Postgres wire protocol)
psycopg[binary]>=3.1

# Config
python-dotenv>=1.0

# CLI + pretty output
typer>=0.12
rich>=13.7

# MCP client (for the recall path) — pin verified in the spike
mcp>=1.0
