[pytest]
# `live` tests hit the real LLM + DB and are skipped unless explicitly selected
# with `-m live`. The default run (deterministic + write-back) needs no API key.
addopts = -m "not live"
markers =
    live: exercises the real LLM/DB (diagnosis-quality puzzles); opt in with -m live
testpaths = tests
