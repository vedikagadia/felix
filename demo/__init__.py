"""felix demo-video pipeline.

Fully automated: `python -m demo.orchestrate` stands up the live stack, drives
the real UI in a recorded browser, narrates it with TTS, and muxes the two into
`demo/out/felix-demo.mp4`. See demo/README.md.
"""
